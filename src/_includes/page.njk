---
layout: default.njk
---

<header class="mb8">
    <h1>
        <a href="/#contents">
            The Design of Forms
            <small class="upper spacing">in Government departments</small></a>
        {%- if section and section != 'Back matter' -%}
            &nbsp;–
            <a class="inline-block upper spacing" href="/#{{ section | lower | replace(' ', '-') }}">
                <small>
                    {{- section -}}
                </small></a>
        {%- endif -%}
        {%- if title -%}
            &nbsp;– 
            <small class="inline-block">
                {{ title }}
            </small>
        {%- endif -%}
    </h1>
</header>

<h1>{{ title }} <span data-status="{{ status }}">{{ status }}</div></h1>

{{ content | safe }}

<hr/>

<h2>Contents</h2>

{% include 'contents.njk' %}
