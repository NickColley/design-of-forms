<!DOCTYPE html>
<html lang="en">
  <head>
    <title>{% if title %}{{ title }} – {% endif %}{% if section %}{{ section }} – {% endif %}The Design of Forms in Government departments</title>
    <meta name="description" content="Digital remaster of the 1940s Design of Forms in Government Departments design manual." />
    <meta charset="utf-8">
    <meta name="theme-color">
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
    <link rel="stylesheet" href="/assets/styles.css">
  </head>
  <body>
    {# Set theme inline to prevent flashing of default theme. -#}
    <script>
      const theme = localStorage.getItem("theme");
      if (theme) document.documentElement.dataset.theme = theme;
    </script>

    <a href="#main" class="skip-link">
      Skip to main content
    </a>
    <a href="#table-of-contents" class="skip-link">
      Skip to table of contents
    </a>

    {{ content | safe }}

    {% include 'partials/footer.njk' %}
    <script src="/assets/scripts.js"></script>
  </body>
</html>
